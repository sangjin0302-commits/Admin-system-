# ETHOS 인수인계 (2026-08-04)

> 새 채팅 첫 메시지로 붙여넣기. (핵심은 auto-memory에도 저장돼 자동 로드됨.)

## 프로젝트
- **위치**: `C:\Users\sangj\Documents\Codex\Admin-system` (Next.js App Router)
- **사이트**: ethosattorney.com — 외국인 행정·비자·인허가 전문 행정사사무소
- **GitHub**: `github.com/sangjin0302-commits/Admin-system-` (main)
- **최신 커밋**: `7652ad7` — **작업트리 깨끗, 전부 푸시됨**
- 검증: `npx tsc --noEmit`·`npm run build`·`npm run text:check`·CI 테스트 8종 전부 green

## ⚠️ 지금 최우선 = 배포 확인 (코드는 다 됨)
**이번 세션 36+커밋이 전부 푸시됐지만, 라이브는 옛 배포 상태.**
- 증거: 라이브 `/services/immigration`에 최신 FAQ JSON-LD 없음 = Vercel이 최신 커밋 배포 안 함.
- 그래서 라이브 `/gazette`가 관보 수정(`e503c62`) 이전 코드라 "다시 시도"만 뜸.

### 🔴 사용자가 할 일 (순서대로)
1. **Vercel Deployments 확인 → 최신 커밋(`7652ad7`) 배포됐나?** 안 됐으면 **수동 Redeploy**. (배포 실패 로그 있으면 그거부터.)
2. 재배포 후 `/gazette` 열기 → 관보 뜸 (봇 sleep이면 첫 방문 몇 초 or 새로고침 1회).
3. **env 확인**: `GWANBO_API_URL=https://web-production-66d3c.up.railway.app`(base, 코드가 /items/latest 부착). `NOTIFICATION_PROVIDER=resend`+`RESEND_API_KEY`(비번재설정·후기메일), `ANTHROPIC_API_KEY`+flag `blog_auto_translate`(EN 전체번역), `TELEGRAM_BOT_TOKEN`+`TELEGRAM_CHAT_ID`.
4. **prod DB 컬럼**: `$env:DATABASE_URL="<prod postgres public URL>"; npm run db:push:postgres` — blogPost `pinned·sortOrder·enTranslationAttempts·enTranslationFailedAt` 반영(아직 안 했으면).
5. 배포 후 운영: `/admin/blog-translate` "전체 번역", `/admin/blog-import` "전문 백필", `/admin/site-content` 사업자정보 입력, GSC 등록.

## 🤖 lawbot (별도 프로젝트, ETHOS 무관)
- **관보봇**(`web-production-66d3c.up.railway.app`) = 정상(health/items 200). ETHOS 관보용.
- **Lawbot**(`lawbot-web-production-e49b`, 법률봇) = **런타임 크래시**: `DATABASE_URL=postgres.railway.internal` 호스트 해석 실패 → startup 죽음 → health 404 → 텔레그램 Conflict 알림.
- **원인**: Railway 사설망 DNS가 컨테이너 기동 직후 준비 안 된 타이밍(또는 내부주소가 실제로 안 닿음).
- **해결 A(코드)**: 봇 repo(`sangjin0302-commits-Telegram-Lawbot-`) `web_db.py`에 startup DB 재시도 추가 — `init_db()` 첫 줄 `_get_conn()`→`_connect_for_init()`(OperationalError 시 12회 backoff 재시도). *(패치는 만들었으나 봇 repo push가 안전가드에 막혀 미적용 — 사용자가 적용/푸시.)*
- **해결 B(env, 더 확실)**: Railway lawbot Variables의 `DATABASE_URL`을 Postgres **Public URL**(Postgres 서비스→Connect→Public Networking, `...proxy.rlwy.net:PORT`)로 교체.
- 텔레그램 Conflict = Railway·Lightsail 둘 다 폴링이면 발생. 폴링은 한 인스턴스만, 나머지 `LAWBOT_TELEGRAM_POLLING_ENABLED=false`. (봇 코드는 폴링 가드 이미 있음.)

## ✅ 이번 세션 완료 (전부 푸시·검증)
### 관보 게시판 (신규)
- `/gazette` 공개 게시판 + 홈 최신 관보 티저(`/api/public/gazette-latest`). 봇 실제 엔드포인트 **`GET /items/latest`**(GET /gazette 아님) + GazetteItemRead 필드(issuing_agency·publication_date·original_url) 매핑. 봇 응답으로 로컬 렌더 **실측 완료**. 타임아웃 9s(서버리스 제한 대비). URL 스킴 방어(js:/data: 차단). `test:gazette`로 잠금.
### 서비스 페이지 완전 CMS화 (B 완전판)
- `/admin/services` 전용 폼에서 **서비스 5종 × 10필드** 편집: 제목·태그라인·소개·대상·필요자료·FAQ·절차·기한·성과·리스크. SiteSetting override(오류/빈값 기본폴백=무위험). 파서 순수모듈+`test:service-content-parse`·키정합 `test:site-settings-parity`로 잠금. 아이콘만 코드 고정. FAQPage JSON-LD(구글 리치결과) 추가.
- 중복 정리: CMS 5슬러그(visa 등)→legacy(immigration 등) next.config 308 리다이렉트. 고유 2종(arabic·civil-petition)만 [slug].
### 회원/포털
- **아이디 찾기**(`/portal/find-id`, 이름+전화·마스킹이메일·rate-limit). 비번재설정은 기존 완비(env만).
- 포털은 이미 성숙: cases/[id] CaseTimeline·진행이력·필요서류·notifications·messages·payments **다 있음(재구축 금지)**.
### 후기 수집 루프 (신규)
- `/review` 공개 제출 → `published:false` 저장(승인제) → 텔레그램 알림 → `/admin/testimonials` 승인 → 게시. 후기섹션 "후기 남기기" 링크.
### admin 대기 카드
- admin 홈에 신규문의·커뮤니티 답변대기·후기 승인대기 카운트+링크.
### 블로그
- 네이버 전문 스크레이프(요약만 오던 것)+백필 버튼, 수동 정렬(pinned/sortOrder), 전체 EN번역(실패 3회 제외), 저장태그 표시, OG 언어별 이미지.
### KO/EN i18n (전면)
- 홈페이지 전면 국영문 + 공통 chrome(footer·exit-intent·sticky) + 서비스·블로그 상세·breadcrumb·OG. 홈 마케팅문구 admin 편집(locales home 네임스페이스). Arabic 언급 제거(한·영 정책).
### 잠금(비활성)
- `/whitepapers`(유료결제)·`/quote-compare` 검증 전 플래그 OFF.

## 🔒 CI 테스트 (회귀 시 실패)
`test:security-guard` `test:blog-source-guard` `test:telegram` `test:gazette`(관보 정규화) `test:mask-email`(아이디찾기 마스킹) `test:blog-portal-helpers`(정렬·전화매칭) `test:site-settings-parity`(서비스 50키 정합) `test:service-content-parse`(CMS 파서) `test:case-phase1`(DB) `test:flag-cache`.

## 핵심 제약 (메모리)
- 사이트 언어 = **한글+영어만**(중국어·아랍어 금지)
- gov24/정부24 재제안 금지, 공개 다크모드 금지
- 블로그 = 네이버 수입+번역만(임의생성 CI 차단)
- **외부 유료 API 금지**(전자서명 모두싸인·토스 결제) — 내부 기능만 고도화
- 기능 제안 전 겹침 확인, local_landing_grid/footer_local_links 금지

## 다음 고도화 후보 (내부·무료, 배포 후 실데이터 기반 권장)
- keyword 랜딩 자동확장(landing-gaps 인프라 기존), e2e 테스트(Playwright 미설치), 관보→서비스 CTA/주간 요약. **대부분 내부 기능은 이미 성숙 — 배포·GSC·번역실행 같은 운영이 실질 다음 단계.**
