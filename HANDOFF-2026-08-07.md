# ETHOS 인수인계 — 2026-08-07 (긴 세션 종료)

프로젝트: **ETHOS 행정사사무소** (Next.js App Router · ethosattorney.com)
- 사이트 repo: github.com/sangjin0302-commits/Admin-system- · main · **최신 `85edf23`**
- 관보 봇 repo: github.com/sangjin0302-commits/Gwanbo-bot · main · **최신 `c4b2c40`**
- 로컬: `C:\Users\sangj\Documents\Codex\Admin-system`, `C:\Users\sangj\Documents\Codex\2026-05-05\Gwanbo-bot`

**두 repo 모두 원격에 전부 푸시 완료. 로컬 working tree clean. 커밋/푸시 정상 작동한다**
(이전에 "안 된다"고 느낀 건 이미 다 커밋·푸시돼 새로 올릴 게 없었기 때문. `git status`가 clean이면 정상).

---

## 🔴 CI(GitHub Actions)가 빨간불인 이유 — 코드 아님

- 로컬에서 **npm ci(exit 0)·typecheck·text:check·잠금 20+종·unit 89·build 전부 통과**. ci.yml YAML도 유효.
- 그런데 CI는 **모든 잡이 로그 하나 없이 즉시 실패**(BlobNotFound). 이건 잡이 **시작조차 못 한다**는 뜻.
- repo가 **PRIVATE** → GitHub Actions 무료 분(월 2000분) 소비. 이번 세션에 run을 수십 번 돌림.
- **결론: Actions 무료 분 소진(또는 spending limit) = 계정 이슈. 코드는 그린.**
- **조치(네 몫)**: GitHub → Settings → Billing and plans → Actions 사용량 확인 → ① 월 리셋 대기, ② spending limit 상향, ③ 또는 repo를 public으로(무료). 분 복구되면 CI 자동 초록.

---

## 🔴 배포 전 필수 (누적 — 아직 안 했으면 반드시)

1. **DB 스키마 push** (새 컬럼/테이블: board·cardNews·cardNewsEn·scheduledAt·CustomerReferral·CustomerReferralUse):
   ```bash
   cd C:\Users\sangj\Documents\Codex\Admin-system
   DATABASE_URL="<prod postgres URL>" npm run db:push:postgres
   ```
   (Railway prod postgres URL. **안 하면 블로그 저장·레퍼럴 페이지 에러**.)
2. **사이트 재배포**(Vercel) — 최신 `85edf23` 반영.
3. **봇 재배포**(Railway) — 최신 `c4b2c40` 반영(관보 통계·전체표시).
4. **환경변수/플래그**:
   - `BLOG_ALLOW_NONNAVER_AUTHORING=true` (Vercel env) — 직접 글쓰기 켜기. **없으면 새 글 작성 시 403.**
   - `/admin/features`에서 기본 OFF 확인: `naver_blog_import`(네이버 자동수입), `blog_auto_translate`(자동 영문번역) — 직접 작성 운영이면 OFF 유지.
   - 주간 관보 통계 받으려면 `gazette_weekly_digest` ON.
   - `GWANBO_API_URL`(봇 주소)·`GWANBO_API_TOKEN`(있으면).

---

## 이번 세션(08-06~07) 완료 — 전부 푸시됨

### 블로그 — 직접 작성 CMS로 전환
- **직접 작성 완비**: 국문+영문(제목/발췌/본문 EN 직접입력)·게시판(폴더, datalist)·카드뉴스(맨끝, 첫장=커버, KO/EN)·**이미지 업로드**(파일→Vercel Blob, 10MB, 개수무제한)·마크다운 서식툴바·**마케팅 골격 템플릿 4종**·태그·**예약 게시**(hourly cron)·**삭제/수정**(에디터+목록 행 버튼).
- **네이버 자동수입·자동번역 기본 OFF**(flag). 직접작성글(source!=NAVER)은 절대 자동번역 안 함.
- admin 블로그 페이지 직접작성 중심 재편("+ 새 글 작성" 상단, 네이버수입은 접힌 선택).
- 용량: 작성 무제한, 표시 공개 5000·admin 2000.
- 중복 근본수정(blogTitleKey 디코딩키), 작성글 공개리스트 노출버그 수정.

### 관보 (봇+사이트)
- 봇 `/items/latest` 10건 고정 → **limit 존중(최대 2000)**. `/items/stats` 통계 API 신설.
- 사이트: 표시 300→1500(사실상 전량), **홈 관보 티저에 통계 배지**(총건수·최근7/30일·기관별).
- **카테고리 전면 제거**(비자 오태깅 해결) — 시간순만. 클릭→원문(original_url) 연결.
- 주간 다이제스트 통계 강화(기관별·기간).

### i18n 경로화 + 정적화(비용/SEO)
- `?lang=en` → **`/en/*` 경로기반**(미들웨어 rewrite + 301). getRequestLocale/i18n-locale/i18n-request.
- **정적화(ISR ○)**: 홈(`/`·`/en` 전체홈)·about·fees·services(5)·cases·consult·contact·careers·quick-check + 각 /en. `STATIC_EN_ROUTES` set + 미들웨어 제외 + 일치 잠금(test:static-en-routes).
- **on-demand revalidation**: 블로그·사이트콘텐츠·i18n·후기·경력·사례·수임료 저장 시 즉시 갱신.
- 남은 동적: blog/[slug]·cases/[slug]·keyword·gazette(감사 권장=ISR 전환, 단 blog/[slug]는 이중언어+noindex+잦은변동으로 복잡).

### 비용/성능/안정
- AI 단일 게이트웨이(isAiAllowed+recordAiSpend, 크론AI 14개 이관, 잠금). 로고 4.2MB→48KB. framer-motion 전역 번들서 제거(CSS화). 크론 시간 재조정+격주게이트+스케줄잠금. COOP/CORP 헤더.

### 버그 수정(전수 감사 발견분)
- 폰트 폴백(font-serif→Georgia였음), 죽은 app-shell(퍼널·채널 분석 32태그 부활), /en 한국어 렌더(services/intake/track), blog/[slug] 500 위험, 죽은 tel placeholder, 예약발행 재발행, /newsletter 404, **칸반 드래그 무저장**, blog_auto_translate 중복+default:true, hreflang/canonical 14페이지.
- brittle 유닛테스트 9개 grep→import 재작성. 레퍼럴 DB화, 뉴스레터 welcome+GA, Toss웹훅 잠금.

---

## 감사 점수(전수조사)
- 마케팅 ~7.5 · 안정성 7.5 · 디자인 8.2 · admin UX 6.0 · GSC 7.5 · Vercel 7.0

## 다음 세션 백로그(우선순위)
1. **자작 KO/EN 장문 콘텐츠**(네 몫) — 오가닉 유입 최대 레버. env 켜고 `/admin/blog/new`에서 직접 작성.
2. **blog/[slug]·cases/[slug]·keyword ISR화** — Vercel 감사 최대 비용/SEO 레버(단 복잡).
3. **GSC page 차원 추가**(getPageAnalytics) — URL별 CTR 최적화. GSC 데이터 저장(추세).
4. **admin UX 개선**: nav 커버리지(160 라우트 URL-only), 이중 헤더 제거, 토스터 2개 통일, dead admin-* 디자인클래스.
5. 이메일 구독자 DB 테이블화(지금 JSON blob), 결제 웹훅 통합테스트, system-health 4중 shim 정리.

## 핵심 운영 노트
- **잠금 20+종 + unit 89 + e2e** CI 하드게이트(분 복구되면). 회귀 차단.
- **AI 킬스위치**: `/admin/features` `ai_master_kill` ON → 전 AI 정지.
- **커밋/푸시**: `git add -A && git commit -m "..." && git push origin main`. `.env` 안 들어가게 `git status` 먼저.
- 상세는 Claude 메모리 `ethos-i18n-path-migration.md`, `ethos-handoff-2026-08.md` 참고.
