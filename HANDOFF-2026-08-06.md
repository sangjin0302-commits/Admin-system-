# ETHOS 인수인계 — 2026-08-06

프로젝트: **ETHOS 행정사사무소** (Next.js App Router · ethosattorney.com)
Repo: github.com/sangjin0302-commits/Admin-system- · main
최신 커밋: `dd23a39` · **CI 게이트 그린**(e2e 83 + lock 20 + unit 77)

---

## 🔴 프로덕션 즉시 조치 (네가 할 것, 순서대로)

1. **Vercel 재배포** — 이 세션의 모든 커밋 반영. (137 빌드OOM 수정돼 이제 빌드 통과함.)
2. **Vercel Pro 유지** + **Settings → Billing → Spend Management**: 금액 입력 + **"Pause Projects" ON** (진짜 하드캡. 알림만 X).
3. **Anthropic 콘솔 → 월 지출 한도** 설정 (AI 비용은 Vercel과 별개 청구!).
4. **크론 확인**: Vercel → Cron Jobs 탭 → content-sync(14:00 KST) Last run. 안 돌면 Pro/플랜 확인.
5. **블로그 데이터 정비** (아래 "정비 실행" 참고) — 기존 중복글 삭제 + 오분류 재분류.
6. **블로그 백필**: `/admin/blog-import → 전체 가져오기` (8월 글 즉시 수입).

---

## 이번 세션(08-05~06) 완료 — 전부 푸시·CI 그린

### 배포/비용
- **137 빌드 OOM 수정**: Sentry 소스맵 업로드를 `SENTRY_AUTH_TOKEN`+org+project 있을때만 + widen off, `productionBrowserSourceMaps:false`, `webpackMemoryOptimizations:true`.
- **AI 비용 방어**: `ai-budget-guard`(flag `ai_master_kill`=1클릭 전면정지 + 월예산 `ai.monthlyBudgetUsd` 기본$10). blog-translation·auto-marketing·ai-chat·quick-check 배선.
- **Speed Insights 제거**(초과과금 애드온).
- **크론 하루 1회 통합**: content-sync 14:00 KST(네이버 1시 발행 후) 단일. 헛돌던 10시·이중 삭제.
- **ISR 6페이지**: franchise·network·vip·find-office·links·ar-card (searchParams 없는 정적 → 함수호출 절감).

### 편집 가능화 (맥스 없어도 자연 편집)
- **홈 마케팅 문구 전부** `/admin/i18n`(home) 편집 가능: 히어로·CTA·5대업무·철학·Why·프로세스·대표소개·혜택·브랜드스토리 (Phase1 스칼라 18 + Phase2 배열 6, 폴백 안전).
- **/admin/site-content**: 히어로제목·신뢰숫자·FAQ·서비스설명.
- content-editor 죽은 키 108줄 정리.

### 보안/품질
- **RBAC**: 고위험 admin 라우트 10개+credentials/[id] `requireRole`(SUPER/MANAGER) + `test:admin-rbac-guard` 잠금.
- **i18n 영문 누수 제거**: CookieConsent·OnboardingTour·MobileBottomNav·헤더/FAB aria·홈 ?lang=en 메타.
- **유닛테스트 러너**(`test:unit`): 71→77 하드게이트. 9개 brittle 소스체크는 EXCLUDE(제품버그 아님, 재작성 대상).
- 고아 컴포넌트 33개 삭제.

### 블로그 버그 (이번에 발견·수정)
- **중복(두 개씩)**: generateSlug 랜덤suffix + dedup가 logNo만 봐서 실행마다 1글 놓쳐 재수입. → dedup에 **제목 fallback** 추가. 실증: 재실행 시 재수입 0.
- **분류 전부 visa**: 스크레이프 본문에 사이드바·관련글 등 비자 도배 chrome(글당 비자키워드 1000+개)이 제목 주제를 압도. → importer를 **제목+요약만으로 분류**. 실증: 51편 중 36편이 corporate/contract/appeal/license로 바로잡힘.
- **PWA 아이콘 깨짐**: manifest가 없는 tracking-*.svg 참조 → logo-*.png로 교체.

### 관보
- **한번에 모두**: limit 60→300, 기본 전체.
- **달력식 넘김**: 월(YYYY-MM) 칩 + 이전/다음 달 화살표. 카테고리·월 필터 상호 보존.
- 관보 CTA·주간 요약·칼럼 소재 큐·근거법령 주입은 이전 세션 완료.

---

## 🛠 블로그 데이터 정비 실행 (기존 데이터 교정)

코드는 **미래**를 고침. **이미 쌓인 prod 데이터**(중복·오분류)는 아래로 교정.

**방법 A — admin 엔드포인트(권장, 안전)**: prod 재배포 후, SUPER 계정으로 POST.
```
POST /api/admin/blog/maintenance   { "task": "reclassify" }            # dry-run(리포트만)
POST /api/admin/blog/maintenance   { "task": "reclassify", "apply": true }
POST /api/admin/blog/maintenance   { "task": "dedup" }                 # dry-run
POST /api/admin/blog/maintenance   { "task": "dedup", "apply": true }  # 중복 삭제
```
(SUPER 인증 필요. apply 기본 false. **dedup apply는 글 삭제=비가역** — dry-run 먼저 확인.)

**방법 B — CLI(prod DATABASE_URL 직접)**:
```
npm run prisma:generate:postgres
DATABASE_URL="<Vercel prod postgres URL>" npx tsx scripts/reclassify-blog-posts.ts          # dry-run
DATABASE_URL="<...>" npx tsx scripts/reclassify-blog-posts.ts --apply
DATABASE_URL="<...>" npx tsx scripts/cleanup-duplicate-blog-posts.ts --apply
```

---

## SEO/블로그 운영 (검토 결과 — 정상)
- **KO 수입글 = noindex + canonical→네이버**(중복콘텐츠 회피, 네이버가 정본).
- **EN 번역 = 색인**(canonical=EN, hreflang). **자작글 = 색인**.
- → 구글엔 EN번역·자작글만 노출(의도된 전략). KO는 네이버 상위 노림.
- **EN 번역은 `ANTHROPIC_API_KEY` + 크론(Pro) 있어야 생성됨.**

---

## 남은 백로그 (다음 세션)
1. **`?lang=` → 경로기반(/en) 전환** — 그러면 홈·서비스·about 등 고트래픽 페이지도 ISR 가능 = 함수비용 대폭↓. 단 대규모(미들웨어·전 페이지·e2e). **가장 큰 비용절감 레버.**
2. **유닛테스트 9개 brittle 재작성** — public-root-gateway·public-track-page·document-lab·immigration-panel·case-matter·lawbot×3·public-marketing. 소스 문자열 grep 방식이라 리팩터마다 깨짐. UI 모델을 직접 import해 검증하는 방식으로 재작성. (제품버그는 아님.)
3. **Vercel Fluid Compute 켜기**(설정) — 함수비용 추가 절감.
4. **관보→블로그 초안 자동화 심화** (지금 골격+요약+근거법령까지).
5. **실물 사진·영문 소셜프루프**(콘텐츠 — 슬롯은 열림: image.aboutPhoto).

---

## 핵심 운영 노트
- **크론은 Vercel Pro 필수**(Hobby는 미실행 → 블로그/자동화 정지가 여기서 옴).
- **하드게이트 잠금 20종 + e2e(83, push게이트) + unit(77)** — 회귀 시 CI 차단. e2e 수동 재실행: `gh workflow run ci.yml --ref main`.
- **편집 시스템 3중**(site-content / i18n / content-editor) — 겹침 주의. 홈은 i18n+site-content 사용.
- **AI 킬스위치**: 비용 폭주 시 `/admin/features`에서 `ai_master_kill` ON → 전 AI 즉시 정지.
- 상세 컨텍스트는 Claude 메모리 `ethos-handoff-2026-08.md` 참고.
